<%= form_with model: [hint, hint_comment], url: hint_hint_comments_path(hint.id), local: false  do |f| %>
  <%= f.text_area :comment, cols: "110", rows: '3', placeholder: "コメントをここに", class: "textarea memory-textarea" %>
  <%= f.submit "送信する", class: "memory-submit" %>
<% end %>