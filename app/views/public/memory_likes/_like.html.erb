<% if user_signed_in? %>
  <% if memory.liked_by?(current_user) %>
    <%= link_to memory_memory_likes_path(memory.id), method: :delete, remote: true do %>
    ♥<%= memory.likes.count %> いいね
    <% end %>
  <% else %>
    <%= link_to memory_memory_likes_path(memory.id), method: :post, remote: true do %>
      ♡<%= memory.likes.count %>いいね
    <% end %>
  <% end %>
<% else %>
   ♥<%= memory.likes.count %> いいね
<% end %>
