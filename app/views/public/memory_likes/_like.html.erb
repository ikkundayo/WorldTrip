<% if user_signed_in? %>
  <% if memory.liked_by?(current_user) %>
    <%= link_to memory_memory_likes_path(memory.id), class: "memory-action-content1 link-change", method: :delete, remote: true do %>
    ♥<%= memory.likes.count %>
    <% end %>
  <% else %>
    <%= link_to memory_memory_likes_path(memory.id), class: "memory-action-content1 link-change", method: :post, remote: true do %>
      ♡<%= memory.likes.count %>
    <% end %>
  <% end %>
<% else %>
   ♥<%= memory.likes.count %>
<% end %>
